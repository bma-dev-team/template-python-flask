flask>=3.0
flask-wtf>=1.2  # CSRF protection + csrf_token() for the shared form macros (_bma_ui.html)
gunicorn>=21.2  # production WSGI server (Railway deploy reads Procfile)
python-dotenv>=1.0
pytest>=8.0
pyyaml>=6.0
cryptography>=42.0  # Fernet-encrypted named-secret store (app/secret_store.py)
# playwright>=1.40  # dev-only: install + `playwright install chromium` to run scripts/walkthrough_screenshots.py

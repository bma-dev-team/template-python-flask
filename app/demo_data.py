"""Per-build synthetic-input generators. REPLACE this stub in each build.

Pure functions fabricating realistic-but-synthetic inputs. Imported by BOTH the
build's pytest suite and app/demo_seed.py so mock inputs are shared, never
duplicated. Nothing here is buyer data.
"""
from __future__ import annotations

# bma-execute-build init scaffolds one generator per declared input artifact.
